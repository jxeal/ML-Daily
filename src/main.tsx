import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// This is a comment to confirm that main.tsx exists in the src folder.
createRoot(document.getElementById("root")!).render(<App />);
